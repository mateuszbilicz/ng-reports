{
  "name": "ng-reports",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo start:dev",
    "build": "turbo run build",
    "build:clean": "rm -rf build && turbo run clean",
    "db:up": "sh ./scripts/start-mongo.sh",
    "install:server": "sh ./scripts/install.sh",
    "swagger": "cd scripts && java -jar swagger-codegen-cli.jar generate -l typescript-angular -i ../apps/api/swagger.json -o ../apps/client/src/app/core/swagger",
    "pack:lib": "sh ./scripts/pack-ng-reports.sh",
    "test": "turbo run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.17.0",
  "devDependencies": {
    "turbo": "^2.6.1"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.12.0"
  }
}